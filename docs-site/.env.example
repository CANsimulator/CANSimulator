# Public site URL — used for canonical URLs, sitemap, and JSON-LD schema.
# Set to your production domain (no trailing slash).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: link to the live CAN simulator app (used in page CTAs).
NEXT_PUBLIC_SIMULATOR_URL=http://localhost:5173
